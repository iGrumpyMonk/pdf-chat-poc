version: "3.9"
services:
  app:
    build: .
    env_file: .env
    ports:
      - "8000:8000"
    volumes:
      - ./documents:/app/documents
      - ./chroma_index:/app/chroma_index
    restart: unless-stopped